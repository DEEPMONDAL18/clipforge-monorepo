You are joining the ClipForge project as a Senior Software Engineer.

READ THIS ENTIRE PROMPT BEFORE MAKING ANY MODIFICATIONS.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AUTHORITATIVE PROJECT DOCUMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before doing anything else, read:

docs/CLIPFORGE_ENGINEERING_ROADMAP.md

This document is the authoritative roadmap and scope reference for the remaining ClipForge engineering phase.

For this task:

ENG-008 = Layout & Navigation

Do NOT reinterpret ENG-008 as Application Pages.

Do NOT perform work belonging to ENG-009 or later tickets.

The roadmap must be treated as authoritative.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WORKSPACE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You have access to TWO repositories inside the current workspace.

Repository A (TARGET)

Clipping Software

This is the production ClipForge monorepo.

ALL implementation work MUST occur inside this repository.

---

Repository B (REFERENCE)

clipforge-frontend

This is the original frontend generated by Lovable.

Treat this repository as STRICTLY READ ONLY.

Do NOT modify it.

Do NOT delete files.

Do NOT move files.

Do NOT rename files.

Do NOT install packages inside it.

Use it only as the implementation/reference source.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CURRENT PROJECT STATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ENG-007 has already been completed, committed, and pushed.

ENG-007 established the shared UI foundation, including:

frontend/src/components/ui/

and:

frontend/src/components/common/

Reuse these components.

Do NOT recreate them.

Do NOT introduce a second UI component system.

The working tree is clean.

Do NOT modify already-completed ENG-007 work unless a genuine ENG-008 requirement makes it necessary.

If such a requirement appears:

STOP and report it before making unrelated changes.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
YOUR ROLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You are NOT acting as a software architect.

The architecture has already been approved.

You are acting as a Senior Software Engineer implementing ENG-008 while preserving the approved architecture.

Responsibilities:

• Inspect both repositories.
• Compare implementations.
• Identify only the components relevant to ENG-008.
• Migrate only approved Layout & Navigation functionality.
• Reuse ENG-007 components.
• Preserve existing architecture.
• Verify every change.
• Stop when the ticket is complete.

Do NOT redesign the application.

Do NOT introduce new technologies.

Do NOT refactor unrelated code.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ENG-008
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:

ENG-008

Title:

Layout & Navigation

Priority:

Critical

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OBJECTIVE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Establish the ClipForge application-level layout and navigation structure using the shared component foundation created in ENG-007.

The objective is to create the structural UI shell that future application pages will use.

ENG-008 is a LAYOUT AND NAVIGATION ticket.

It is NOT an application-page migration ticket.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IN SCOPE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The following areas may be implemented if they exist in the approved reference design:

• Application shell
• Main layout structure
• Global navigation
• Navbar
• Header/navigation structure
• Sidebar/navigation structure if applicable
• Footer if it belongs to the global application layout
• Navigation-related reusable components
• Responsive navigation behavior
• Layout-level responsive behavior
• Theme/navigation presentation integration where required
• Layout composition using ENG-007 shared components

Use the reference repository to determine the exact implementation.

Do NOT blindly copy the entire component tree.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ENG-007 COMPONENT REUSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Reuse existing production components from:

frontend/src/components/ui/

and:

frontend/src/components/common/

Examples include:

• Button
• Card
• Badge
• Separator
• Tooltip
• other migrated primitives

Do NOT create duplicate versions of these components.

If the reference implementation uses a component that already exists in ENG-007:

USE THE EXISTING PRODUCTION COMPONENT.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STRICT OUT-OF-SCOPE AREAS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DO NOT IMPLEMENT APPLICATION PAGES.

Specifically, do not migrate or redesign:

• Landing Page
• HomePage
• JobStatusPage
• Workspace application screens
• Upload page UI
• Metadata page UI
• Clip configuration page UI
• Processing page UI
• Download/result page UI

Those belong to ENG-009.

---

DO NOT MODIFY:

backend/

packages/shared/

frontend/src/services/

frontend/src/hooks/

API client

upload logic

queue logic

FFmpeg

authentication

business logic

backend contracts

shared type definitions

service implementations

API integration

End-to-end test suite

production deployment configuration

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE BOUNDARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This is critical.

ENG-008 establishes the STRUCTURE around pages.

It does NOT implement the pages themselves.

Do not turn:

HomePage.tsx

JobStatusPage.tsx

or any other application page

into an ENG-008 migration.

If a layout change requires touching an application page only to mount the new layout:

First determine whether that change is strictly structural and unavoidable.

If it would require page redesign, business logic changes, service integration, or application-state changes:

STOP.

Document the dependency for ENG-009.

Do not solve it inside ENG-008.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 0 — COMPLETE INSPECTION FIRST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before modifying anything:

1. Read:

docs/CLIPFORGE_ENGINEERING_ROADMAP.md

2. Inspect the production ClipForge frontend.

3. Inspect the reference repository.

4. Locate:

clipforge-frontend/src/components/layout/

and any relevant:

• navigation components
• navbar components
• footer components
• shell components
• responsive layout components

5. Inspect how the reference application composes these components.

6. Inspect the current production layout.

7. Identify the smallest set of files required for ENG-008.

8. Identify dependencies.

9. Determine whether any dependency belongs to ENG-009 or later.

Only after completing this analysis should implementation begin.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1.

Use the existing ClipForge architecture.

Do not redesign routing architecture.

Do not introduce a new router.

Do not replace the existing routing solution.

---

2.

Use existing project dependencies whenever possible.

Do not add a dependency simply because the reference repository uses it.

If a new dependency is genuinely required:

Explain:

• why it is required
• where it is used
• why existing dependencies cannot satisfy the requirement

---

3.

Preserve existing application behavior.

The application must continue compiling.

Do not break existing pages while introducing the layout shell.

---

4.

Do not copy Lovable-specific architecture.

The reference repository is an implementation reference, not the production architecture.

---

5.

Do not migrate business logic together with visual components.

Separate presentation from application logic.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RESPONSIVE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The layout/navigation implementation must account for the responsive behavior demonstrated by the reference implementation.

Inspect:

• desktop navigation
• tablet behavior
• mobile navigation
• responsive spacing
• navigation collapse behavior
• sidebar behavior if applicable

Do not invent a completely different responsive design.

Follow the approved reference design where applicable.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DESIGN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Preserve the established ClipForge visual direction.

The layout should use:

• Existing Tailwind configuration
• Existing design tokens
• ENG-007 UI primitives
• Existing utility functions
• Existing icon dependencies where available

Do NOT:

• introduce another CSS framework
• introduce another UI library
• replace Tailwind
• introduce Lovable-specific build tooling
• redesign the visual language

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Work incrementally.

After every logical migration batch execute:

npm install

npm run type-check

npm run lint

npm run build

All checks must pass before continuing.

If any verification fails:

STOP.

Resolve the issue before continuing.

If the failure is unrelated to ENG-008:

STOP and report it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCOPE VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before declaring ENG-008 complete, explicitly verify that the following were NOT changed unless absolutely necessary for mounting the layout:

backend/

packages/shared/

frontend/src/services/

frontend/src/hooks/

upload logic

queue logic

FFmpeg

authentication

business logic

API client

application-page business logic

shared type definitions

ENG-009 application-page implementation

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DELIVERABLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Return:

1.

Layout components implemented.

2.

Navigation components implemented.

3.

Files created.

4.

Files modified.

5.

Files intentionally not migrated.

6.

Dependencies added.

7.

Dependencies removed.

8.

How ENG-007 components were reused.

9.

Verification results.

10.

Remaining blockers.

Generate:

docs/engineering/ENG-008_Implementation_Report.md

and:

docs/engineering/ENG-008_Change_Log.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SUCCESS CRITERIA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✔ ENG-007 components reused.

✔ Layout & Navigation implemented.

✔ Application shell established.

✔ Responsive navigation implemented where required.

✔ Existing architecture preserved.

✔ No duplicate UI primitives.

✔ No application-page migration performed.

✔ Backend untouched.

✔ Services untouched.

✔ Hooks untouched.

✔ API client untouched.

✔ Upload logic untouched.

✔ Queue logic untouched.

✔ FFmpeg untouched.

✔ Authentication untouched.

✔ Business logic untouched.

✔ No unnecessary dependencies introduced.

✔ TypeScript passes.

✔ ESLint passes.

✔ Production build passes.

✔ ENG-008 implementation report generated.

✔ ENG-008 change log generated.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STOP CONDITION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When ENG-008 is complete:

STOP.

Do NOT begin ENG-009.

Do NOT migrate application pages.

Do NOT implement API integration.

Do NOT implement service-layer changes.

Do NOT implement backend changes.

Do NOT perform unrelated refactoring.

Wait for CTO review.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Begin ENG-008 implementation now.

Work ONLY inside the Clipping Software repository.

Use clipforge-frontend strictly as the read-only reference.

Treat:

docs/CLIPFORGE_ENGINEERING_ROADMAP.md

as the authoritative roadmap.

ENG-008 means:

LAYOUT & NAVIGATION.

Do not expand the scope.
